head

body#LoginForm
    .container
        .login-form
            .main-div
                .panel
                    if info && info.message
                        div.alert.alert-danger 
                            =info.message
                    h2 Recuperacion de password
                    p Revise ingrese su cuenta de email, le llegarán las instrucciones de reseteo
                form#login(action='/forgotPassword' method='post')
                    .form-group
                        input#email.form-control(type='email' placeholder='Email Adress' name='email')
                    button.btn.btn-primary(type='submit') Resetear